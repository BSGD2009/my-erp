FROM node:20-alpine

# openssl is required by Prisma's query engine on Alpine Linux
RUN apk add --no-cache openssl

WORKDIR /app

# Install dependencies first (cached layer unless package.json changes)
COPY package*.json ./
RUN npm install

# Source is mounted as a volume at runtime — no COPY needed for dev
CMD ["npm", "run", "dev"]
